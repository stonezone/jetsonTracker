# WAVECAM vision-only testbed
# --- core ---
# Major-version pins per the rig's installed set (pip freeze 2026-06-11:
# numpy 1.26.4, fastapi 0.136.3, uvicorn 0.48.0, pydantic 2.13.4) — a future
# `pip install -r` on the Orin must not silently jump a major and break tracking.
numpy<3
pyyaml
fastapi<1
uvicorn[standard]<1
pydantic>=2,<3

# --- vision ---
# On a Jetson (JetPack 6.2.x) prefer the SYSTEM OpenCV (CUDA build) over pip.
# If you must pip-install on desktop: opencv-python
opencv-python ; platform_machine != "aarch64"

# --- detector ---
# Ultralytics pulls torch. DO NOT `pip install torch` blindly on Jetson —
# use NVIDIA's JetPack 6.2 (cu126) torch wheel, then `pip install ultralytics~=8.3        # rig: 8.3.233 (2026-06-11)`.
# See README. On desktop, plain `pip install ultralytics~=8.3        # rig: 8.3.233 (2026-06-11)` is fine.
ultralytics~=8.3        # rig: 8.3.233 (2026-06-11)
