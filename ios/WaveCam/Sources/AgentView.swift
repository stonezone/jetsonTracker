import SwiftUI

/// Agent panel: deterministic supervisor health + on-demand Codex (supervise-only).
struct AgentView: View {
    var body: some View {
        ScreenStub(title: "Agent", subtitle: "supervisor + Codex — wiring next")
    }
}
