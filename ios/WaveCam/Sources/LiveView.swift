import SwiftUI

/// Live/Monitor: MJPEG feed + tracking HUD + Emergency Stop. Wired in the next increment.
struct LiveView: View {
    var body: some View {
        ScreenStub(title: "Live", subtitle: "feed + HUD — wiring next")
    }
}
