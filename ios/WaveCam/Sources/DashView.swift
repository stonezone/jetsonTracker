import SwiftUI

/// Dashboard: WKWebView of the Orin dashboard (the rich operator surface).
struct DashView: View {
    var body: some View {
        ScreenStub(title: "Dash", subtitle: "WKWebView of Orin dashboard — wiring next")
    }
}
