import SwiftUI

/// Calibration wizard: preflight → base-lock → heading → tilt → zoom → dry-run.
struct CalibrateView: View {
    var body: some View {
        ScreenStub(title: "Calibrate", subtitle: "setup wizard — wiring next")
    }
}
