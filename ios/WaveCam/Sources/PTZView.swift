import SwiftUI

/// Manual PTZ: joystick (velocity + deadman) + zoom. Wired in a later increment.
struct PTZView: View {
    var body: some View {
        ScreenStub(title: "PTZ", subtitle: "joystick + zoom — wiring next")
    }
}
